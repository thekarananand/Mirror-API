apiVersion: networking.istio.io/v1
kind: Gateway
metadata:
  name: user-gw
  namespace: istio-system
spec:
  selector:
    istio: ingressgateway
  servers:
  - port:
      number: 2001
      name: user
      protocol: HTTP
    hosts:
    - "*"
